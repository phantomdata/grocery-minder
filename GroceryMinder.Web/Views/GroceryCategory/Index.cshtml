@model IList<GroceryMinder.Domain.Models.GroceryCategory>
@{
    ViewBag.Title = "Index";
}

@Html.ActionLink("Create", "Create", null, new { @class = "btn btn-primary" } )
<h2>Index</h2>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>
                    @Html.ActionLink("Update", "Update", new { id = item.GroceryCategoryId }, new { @class = "btn btn-default"})
                    @Html.ActionLink("Delete", "Delete", new { id = item.GroceryCategoryId }, new { @class = "btn btn-default delete" })
                </td>
            </tr>
        }
    </tbody>
</table>

@if (Model.Count == 0)
{
    <div class="alert alert-info">
        <p>You have no items.  Click create above to add some!</p>
    </div>
}